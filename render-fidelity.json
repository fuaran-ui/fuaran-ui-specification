{
  "version": 1,
  "$id": "https://fuaran.dev/wire-format/v1/render-fidelity.json",
  "description": "Per-NodeKind render-fidelity declaration for the Fuaran UI wire format. For each canonical kind.$type: what the wire carries (source), what the parity-checked render pins (fallback), and what, if anything, is declared client-only rich. Generated from Fuaran.UI.RenderFidelity; it transcribes the shipped fidelity contracts and introduces no wire or renderer behaviour. A consumer deriving per-node fidelity badges reads this rather than hand-annotating. Each kind also declares the checkable render obligations it owes, drawn from the closed obligationVocabulary and bound to the spec section that states each one; a host render suite asserts every obligation for the kinds it renders and REPORTS any it cannot check, because not checked is not passed. See WIRE_FORMAT.md 13.",
  "tiers": [
    {
      "tier": "source",
      "meaning": "the deterministic, parity-clean data the wire carries; never a rendered form"
    },
    {
      "tier": "fallback",
      "meaning": "the deterministic render the SSR-parity corpus and the cross-host byte-diff compare - what a no-JS reader, a crawler, or a non-browser host gets"
    },
    {
      "tier": "rich",
      "meaning": "the declared client-only render, explicitly outside every parity comparison rather than silently divergent"
    }
  ],
  "obligationVocabulary": [
    {
      "id": "accessible-name-always",
      "meaning": "an accessible name is emitted on every instance of the kind, never only where an author supplied one"
    },
    {
      "id": "autoplay-muted-pairing",
      "meaning": "autoplay is emitted only together with muted; neither attribute ever appears without the other"
    },
    {
      "id": "no-autoplay-pathway",
      "meaning": "a variant declaring no autoplay slot emits no autoplay pathway at all - neither the attribute nor its pair"
    },
    {
      "id": "refused-source-dropped",
      "meaning": "a secondary source the URL-scheme and egress floor refuses is dropped rather than emitted at the refusal URL"
    },
    {
      "id": "alt-always-emitted",
      "meaning": "the alternative-text attribute is emitted always, the empty string included - absent and empty are different claims"
    },
    {
      "id": "anchor-affordance-on-expandable",
      "meaning": "a declared expansion emits a real working anchor to the full-size asset, honoured with no script at all"
    },
    {
      "id": "refused-src-no-affordance",
      "meaning": "a source the egress floor refused emits no affordance - an expansion that cannot be honoured is worse than none"
    },
    {
      "id": "figure-caption-outside-link",
      "meaning": "the caption sits outside the expansion anchor, so the caption text is not part of the link target"
    },
    {
      "id": "srcset-ascending-by-width",
      "meaning": "responsive candidates are emitted in ascending width order, and a refused candidate is dropped rather than emitted"
    },
    {
      "id": "unregistered-custom-labelled",
      "meaning": "an unregistered custom node with an available contract card renders a labelled placeholder derived from that card - identity, summary and a machine-readable verdict marker - never a blank and never a guess"
    },
    {
      "id": "authored-child-order",
      "meaning": "repeated children are emitted in the authored order the wire carries, never re-sorted"
    },
    {
      "id": "single-default-per-kind",
      "meaning": "at most one repeated child per declared kind carries the default marker; a later duplicate election is emitted without it"
    },
    {
      "id": "transcript-disclosure-named",
      "meaning": "a declared transcript renders as a disclosure beside the transport, carrying its own accessible name"
    },
    {
      "id": "sandbox-always-exactly-declared",
      "meaning": "the sandbox declaration is emitted on every instance, empty when nothing is granted, and carries exactly the declared relaxations - de-duplicated, in declaration order, and never a token the document did not name"
    },
    {
      "id": "refused-embed-source-omitted",
      "meaning": "a primary source the egress floor refuses omits the source attribute entirely rather than pointing the browsing context at the refusal URL, while still recording the refusal"
    },
    {
      "id": "picker-always-present",
      "meaning": "a declared ingress gesture is additional: the file picker and its label are emitted whatever the document declares, so the keyboard-accessible route survives and a no-script host renders a working upload"
    },
    {
      "id": "aria-modal-only-when-blocking",
      "meaning": "the aria-modal inertness claim is emitted for the blocking modality alone; a non-blocking anchored surface carries the dialog role without it, because the page behind it is genuinely still available"
    }
  ],
  "kinds": [
    {
      "kind": "Badge",
      "sensitive": false,
      "source": "the label TextSource + tone",
      "fallback": "a `fuaran-badge` span carrying the resolved label and tone class",
      "rich": {
        "class": "none",
        "meaning": "no client-only tier - the parity-checked fallback is the whole render"
      },
      "fixtures": [],
      "obligations": [],
      "contract": "WIRE_FORMAT.md 3.2; docs/SSR.md (per-kind server behaviour)"
    },
    {
      "kind": "Box",
      "sensitive": false,
      "source": "the child list + layout spec",
      "fallback": "the full structural container, classes identical on both sides; the layout mode's declared fill direction realised per WIRE_FORMAT §3.6.7 (row-fill for `Grid`, column-fill for `Masonry`)",
      "rich": {
        "class": "none",
        "meaning": "no client-only tier - the parity-checked fallback is the whole render"
      },
      "fixtures": [],
      "obligations": [],
      "contract": "WIRE_FORMAT.md 3.2; docs/SSR.md (per-kind server behaviour)"
    },
    {
      "kind": "Button",
      "sensitive": false,
      "source": "the label, variant, disabled binding and the declared action",
      "fallback": "the button element with its classes, resolved label and `disabled` state, carrying no event handlers",
      "rich": {
        "class": "behavioural",
        "enhancement": "the declared action is wired on hydration",
        "seam": "the control is inert server-side and gains behaviour, not structure, at hydration (Phase 143)",
        "meaning": "attached on hydration; adds behaviour, never DOM - it cannot cause a hydration mismatch, so it stays inside the parity contract"
      },
      "fixtures": [],
      "obligations": [],
      "contract": "docs/SSR.md (Input - rendered inert)"
    },
    {
      "kind": "Callout",
      "sensitive": false,
      "source": "the callout TextSource + tone",
      "fallback": "the full structural callout element, tone class and ARIA identical on both sides",
      "rich": {
        "class": "none",
        "meaning": "no client-only tier - the parity-checked fallback is the whole render"
      },
      "fixtures": [],
      "obligations": [],
      "contract": "WIRE_FORMAT.md 3.2; docs/SSR.md (per-kind server behaviour)"
    },
    {
      "kind": "Chart",
      "sensitive": true,
      "source": "the chart kind, the row-source binding, and the axis / legend / value-format declarations",
      "fallback": "for a lowered kind over resolved rows, the SAME first-party lowered `Drawing` SVG the client emits (byte-parity via one shared lowering); otherwise a deterministic `fuaran-chart-ssr-placeholder` carrying `data-fuaran-row-count` and the title - never a blank",
      "rich": {
        "class": "clientOnly",
        "technique": "the client chart library draws into the placeholder",
        "seam": "hydration, for the not-yet-lowered kinds only; a lowered kind has no client-only tier",
        "meaning": "changes the DOM after hydration and is excluded from every parity comparison by contract"
      },
      "fixtures": [
        "nodes/chart-1.json"
      ],
      "obligations": [],
      "contract": "Phase 526; docs/SSR.md (Visualisation)"
    },
    {
      "kind": "CodeBlock",
      "sensitive": true,
      "source": "the code text, a `language` tag, optional `lineNumbers` / `highlightLines`, and `copyable` - deterministic data on the wire",
      "fallback": "a deterministic HTML-escaped `<pre><code class=\"language-{x}\">` with `data-language`, the optional `data-highlight-lines`, and a structurally-present copy button; no markdown library on either side",
      "rich": {
        "class": "clientOnly",
        "technique": "syntax highlighting",
        "seam": "a post-hydration pass targeting `.language-{x}`; a host integration seam, never emitted by a renderer",
        "meaning": "changes the DOM after hydration and is excluded from every parity comparison by contract"
      },
      "fixtures": [
        "nodes/code-1.json"
      ],
      "obligations": [],
      "contract": "Phase 290; WIRE_FORMAT.md 3.2; docs/SSR.md (deterministic-render + client-only-enhancement contract)"
    },
    {
      "kind": "Custom",
      "sensitive": true,
      "source": "the component name, a bounded prop map, and the optional content-identity envelope",
      "fallback": "whatever the host's SERVER custom-renderer registry emits for the component; an unregistered node renders a labelled placeholder, identical on both sides — derived from the contract card (WIRE_FORMAT §25) where one is available, and identity-only where none is",
      "rich": {
        "class": "clientOnly",
        "technique": "the host's registered client renderer",
        "seam": "the `IFuaranRuntime.RegisterCustomRenderer` trust boundary (Phase 141) - the sanctioned escape a host uses for a library render such as a diagram",
        "meaning": "changes the DOM after hydration and is excluded from every parity comparison by contract"
      },
      "fixtures": [
        "nodes/custom-1.json"
      ],
      "obligations": [
        {
          "id": "unregistered-custom-labelled",
          "statement": "where no renderer is registered for a Custom node and a contract card for its identity is available, the emitted placeholder carries the component identity, the card's summary where the card declares one, and a machine-readable verdict marker; it emits no prop value and no guess at the component's appearance, and where the card's content hash contradicts the node's it withholds the description rather than showing one that describes a different shape",
          "section": "WIRE_FORMAT.md 25.4"
        }
      ],
      "contract": "Phase 141; Phase 1108; docs/SSR.md (custom-renderer registry); WIRE_FORMAT.md 25; SANITIZATION.md"
    },
    {
      "kind": "DataGrid",
      "sensitive": true,
      "source": "the column declarations, the row-source binding, and the declared sort / page / edit intent",
      "fallback": "in static read-only mode the full semantic `<table class=\"fuaran-table\">` (byte-identical to the retired `Table`, with the declared sort surfaced as `data-fuaran-sort-*`); for a client-library grid a deterministic `fuaran-grid-ssr-placeholder` carrying `data-fuaran-row-count` - never a blank",
      "rich": {
        "class": "clientOnly",
        "technique": "the client grid library draws into the placeholder",
        "seam": "hydration, for the client-library form only; a `staticRows` grid has no client-only tier",
        "meaning": "changes the DOM after hydration and is excluded from every parity comparison by contract"
      },
      "fixtures": [
        "nodes/grid-1.json"
      ],
      "obligations": [],
      "contract": "Phase 393; docs/SSR.md (Visualisation, sortable rendered tables)"
    },
    {
      "kind": "Disclosure",
      "sensitive": false,
      "source": "the summary, the child tree, and the open binding",
      "fallback": "a native `<details>`/`<summary>` whose `open` attribute reflects the resolved binding - the disclosure opens and closes with no JavaScript at all",
      "rich": {
        "class": "none",
        "meaning": "no client-only tier - the parity-checked fallback is the whole render"
      },
      "fixtures": [],
      "obligations": [],
      "contract": "docs/SSR.md (Layout.Disclosure)"
    },
    {
      "kind": "Drawing",
      "sensitive": false,
      "source": "the declared shape / label geometry",
      "fallback": "a deterministic SVG built by the shared `DrawingSvg` builder, byte-identical on both sides",
      "rich": {
        "class": "none",
        "meaning": "no client-only tier - the parity-checked fallback is the whole render"
      },
      "fixtures": [],
      "obligations": [],
      "contract": "WIRE_FORMAT.md 3.2; docs/SSR.md (per-kind server behaviour)"
    },
    {
      "kind": "ErrorBoundary",
      "sensitive": false,
      "source": "the protected child subtree and the fallback subtree",
      "fallback": "the protected child subtree rendered directly; the fallback is a client-runtime degradation path (the server has no throws to catch)",
      "rich": {
        "class": "none",
        "meaning": "no client-only tier - the parity-checked fallback is the whole render"
      },
      "fixtures": [],
      "obligations": [],
      "contract": "WIRE_FORMAT.md 3.2; docs/SSR.md (per-kind server behaviour)"
    },
    {
      "kind": "Fact",
      "sensitive": false,
      "source": "the label / value TextSources",
      "fallback": "the resolved, formatted fact row",
      "rich": {
        "class": "none",
        "meaning": "no client-only tier - the parity-checked fallback is the whole render"
      },
      "fixtures": [],
      "obligations": [],
      "contract": "WIRE_FORMAT.md 3.2; docs/SSR.md (per-kind server behaviour)"
    },
    {
      "kind": "FileUpload",
      "sensitive": false,
      "source": "the accept / multiple declarations, the label, and the two Phase 1115 ingress declarations (`dropTarget` / `acceptPaste`)",
      "fallback": "the file control with its classes and `disabled` state, carrying no event handlers. The ingress declarations degrade to the PLAIN PICKER: a drop needs a `drop` listener and a paste needs a `paste` one, and no CSS observes a drag, so a static host emits the control it always emitted and marks each declared gesture with a `data-fuaran-upload-drop` / `data-fuaran-upload-paste` attribute recording that the declaration was read - the marker is NOT coverage and nothing in this tier acts on it",
      "rich": {
        "class": "behavioural",
        "enhancement": "selection handling is wired on hydration, and a client tier honouring `dropTarget` / `acceptPaste` writes dropped or pasted files into the control's OWN input so the same selection path and the same `Accept` filter run whatever route the file arrived by",
        "seam": "the control is inert server-side (Phase 143); the `onSelect` payload is non-scalar and needs host wiring",
        "meaning": "attached on hydration; adds behaviour, never DOM - it cannot cause a hydration mismatch, so it stays inside the parity contract"
      },
      "fixtures": [
        "nodes/upload-1.json",
        "nodes/upload-drop-1.json",
        "nodes/upload-paste-1.json"
      ],
      "obligations": [
        {
          "id": "picker-always-present",
          "statement": "the `<input type=\"file\">` and its label are emitted whatever gestures the document declares - a drop zone is an ADDITIONAL route and never a replacement, which is what keeps the keyboard-accessible route intact and what makes the no-script floor a working upload rather than an inert box",
          "section": "WIRE_FORMAT.md 3.6.10"
        }
      ],
      "contract": "Phase 1115; WIRE_FORMAT.md 3.6.10; docs/SSR.md (Input - rendered inert)"
    },
    {
      "kind": "Filters",
      "sensitive": false,
      "source": "the filter item declarations and their bound stores",
      "fallback": "the filter controls with their classes and resolved values, carrying no event handlers",
      "rich": {
        "class": "behavioural",
        "enhancement": "filter write-back is wired on hydration",
        "seam": "the control is inert server-side (Phase 143); the declarative floor is write-back to the filter's own store",
        "meaning": "attached on hydration; adds behaviour, never DOM - it cannot cause a hydration mismatch, so it stays inside the parity contract"
      },
      "fixtures": [],
      "obligations": [],
      "contract": "docs/SSR.md (Input - rendered inert)"
    },
    {
      "kind": "Form",
      "sensitive": false,
      "source": "the field declarations, their bound values and the submit action",
      "fallback": "the form and its fields with their classes, resolved values and `disabled` state, carrying no event handlers",
      "rich": {
        "class": "behavioural",
        "enhancement": "field write-back and submit are wired on hydration",
        "seam": "the controls are inert server-side (Phase 143)",
        "meaning": "attached on hydration; adds behaviour, never DOM - it cannot cause a hydration mismatch, so it stays inside the parity contract"
      },
      "fixtures": [],
      "obligations": [],
      "contract": "docs/SSR.md (Input - rendered inert)"
    },
    {
      "kind": "FragmentDecl",
      "sensitive": false,
      "source": "the fragment template and its hole declarations",
      "fallback": "zero paint - the declaration is a template, not a rendered node",
      "rich": {
        "class": "none",
        "meaning": "no client-only tier - the parity-checked fallback is the whole render"
      },
      "fixtures": [],
      "obligations": [],
      "contract": "WIRE_FORMAT.md 3.2; docs/SSR.md (per-kind server behaviour)"
    },
    {
      "kind": "FragmentRef",
      "sensitive": false,
      "source": "the fragment name and its argument bindings",
      "fallback": "the fragment expanded against the one-shot registry collected from the tree; an unresolved reference renders a labelled placeholder identically on both sides",
      "rich": {
        "class": "none",
        "meaning": "no client-only tier - the parity-checked fallback is the whole render"
      },
      "fixtures": [],
      "obligations": [],
      "contract": "WIRE_FORMAT.md 3.2; docs/SSR.md (per-kind server behaviour)"
    },
    {
      "kind": "Heading",
      "sensitive": false,
      "source": "the heading TextSource + level",
      "fallback": "the full structural heading element",
      "rich": {
        "class": "none",
        "meaning": "no client-only tier - the parity-checked fallback is the whole render"
      },
      "fixtures": [],
      "obligations": [],
      "contract": "WIRE_FORMAT.md 3.2; docs/SSR.md (per-kind server behaviour)"
    },
    {
      "kind": "Icon",
      "sensitive": false,
      "source": "the icon name + size",
      "fallback": "the resolved icon markup through the uniform icon hook",
      "rich": {
        "class": "none",
        "meaning": "no client-only tier - the parity-checked fallback is the whole render"
      },
      "fixtures": [],
      "obligations": [],
      "contract": "WIRE_FORMAT.md 3.2; docs/SSR.md (per-kind server behaviour)"
    },
    {
      "kind": "Image",
      "sensitive": true,
      "source": "the src, alt, variant, the fit / aspectRatio / loading presentation tokens, the optional caption, the srcSet candidate list, and the `expandable` declaration",
      "fallback": "a real `<img>` with the sanitised `src` (unknown or dangerous schemes collapse to `about:blank`), `alt` always emitted, the per-variant class, and the per-token fit / aspect classes plus `loading=\"lazy\"` under `loading = Lazy`. The aspect box is reserved by CSS alone, so the space is held in the server-rendered output with no script. A `caption` wraps the emission in `<figure class=\"fuaran-image-figure\">` with the resolved text in a `<figcaption>`; with no caption there is no wrapper at all. A non-empty `srcSet` emits `srcset` with `<url> <width>w` candidates ordered ASCENDING by width plus `sizes=\"100vw\"`; every candidate's url passes the same URL-scheme + egress floor as `src`, and one that fails it is dropped from the list rather than emitted, so the primary `src` remains the fallback. An empty `srcSet` emits neither attribute. Under `expandable` the `<img>` is wrapped in a real `<a class=\"fuaran-image-expand\" href=\"<the sanitised src>\" data-fuaran-expandable>` — a WORKING link to the full-size asset with no script, which is the whole no-JS story — nested INSIDE the `<figure>` so a caption is not part of the link target; a `src` the egress floor refused emits no anchor at all, because an affordance that cannot be honoured is worse than none",
      "rich": {
        "class": "clientOnly",
        "technique": "the in-page lightbox overlay",
        "seam": "a post-hydration pass targeting `[data-fuaran-expandable]` (the packaged `content/fuaran-image-expand.js`, or the `@fuaran-ui/renderer/enhance-expandable` module); it appends an overlay to `document.body` and suppresses the anchor's navigation only once that overlay is up, so a failure leaves the working link",
        "meaning": "changes the DOM after hydration and is excluded from every parity comparison by contract"
      },
      "fixtures": [
        "nodes/image-expandable-1.json",
        "nodes/image-expandable-figure-1.json"
      ],
      "obligations": [
        {
          "id": "alt-always-emitted",
          "statement": "`alt` is emitted on every image, the empty string included — a decorative image declares an empty alt rather than omitting the attribute",
          "section": "WIRE_FORMAT.md 3.6.2"
        },
        {
          "id": "anchor-affordance-on-expandable",
          "statement": "under `expandable` the `<img>` is wrapped in a real `<a class=\"fuaran-image-expand\" href=\"<the sanitised src>\" data-fuaran-expandable>` — a WORKING link to the full-size asset with no script",
          "section": "WIRE_FORMAT.md 3.6.5"
        },
        {
          "id": "refused-src-no-affordance",
          "statement": "a `src` the URL-scheme + egress floor refused emits no anchor at all, because an affordance that cannot be honoured is worse than none",
          "section": "WIRE_FORMAT.md 3.6.5"
        },
        {
          "id": "figure-caption-outside-link",
          "statement": "with a caption the anchor nests INSIDE the `<figure>` — `<figure>` wraps `<a>` wraps `<img>`, and the `<figcaption>` follows the anchor — so the caption is not part of the link target",
          "section": "WIRE_FORMAT.md 3.6.3, 3.6.5"
        },
        {
          "id": "srcset-ascending-by-width",
          "statement": "a non-empty `srcSet` emits `<url> <width>w` candidates ordered ASCENDING by width; a candidate the egress floor refuses is dropped rather than emitted, so the primary `src` remains the fallback",
          "section": "WIRE_FORMAT.md 3.6.4"
        }
      ],
      "contract": "Phase 1079; WIRE_FORMAT.md 3.6.5; docs/SSR.md (expandable images)"
    },
    {
      "kind": "LabelValueRow",
      "sensitive": false,
      "source": "the label / value TextSources",
      "fallback": "the resolved, formatted row",
      "rich": {
        "class": "none",
        "meaning": "no client-only tier - the parity-checked fallback is the whole render"
      },
      "fixtures": [],
      "obligations": [],
      "contract": "WIRE_FORMAT.md 3.2; docs/SSR.md (per-kind server behaviour)"
    },
    {
      "kind": "Link",
      "sensitive": false,
      "source": "the href, label, and the optional `protection` declaration",
      "fallback": "a real crawlable `<a href>` with a sanitised href - the no-JS navigation path. Under `protection = email` the SSR side emits every character of the href and label as decimal HTML entities while the client sets the decoded href, so the two agree POST-ENTITY-DECODE rather than byte-for-byte; that narrowing is the declared contract, not a divergence",
      "rich": {
        "class": "none",
        "meaning": "no client-only tier - the parity-checked fallback is the whole render"
      },
      "fixtures": [
        "nodes/link-protected-1.json"
      ],
      "obligations": [],
      "contract": "Phase 812; WIRE_FORMAT.md 3.2 (link protection); docs/SSR.md (protected email links)"
    },
    {
      "kind": "List",
      "sensitive": false,
      "source": "the item TextSources + ordered flag",
      "fallback": "an `<ol>`/`<ul>` of `<li>`, classes identical on both sides",
      "rich": {
        "class": "none",
        "meaning": "no client-only tier - the parity-checked fallback is the whole render"
      },
      "fixtures": [],
      "obligations": [],
      "contract": "WIRE_FORMAT.md 3.2; docs/SSR.md (per-kind server behaviour)"
    },
    {
      "kind": "Map",
      "sensitive": true,
      "source": "the marker source binding and the map spec",
      "fallback": "a deterministic `fuaran-map-ssr-placeholder` carrying `data-fuaran-marker-count` - never a blank",
      "rich": {
        "class": "clientOnly",
        "technique": "the client map library draws into the placeholder",
        "seam": "hydration",
        "meaning": "changes the DOM after hydration and is excluded from every parity comparison by contract"
      },
      "fixtures": [
        "nodes/map-1.json"
      ],
      "obligations": [],
      "contract": "docs/SSR.md (Visualisation)"
    },
    {
      "kind": "Markdown",
      "sensitive": true,
      "source": "the raw markdown text as a `TextSource`. Markdown is NEVER parsed into the wire format, so a markdown feature change is not a wire change (WIRE_FORMAT.md 14)",
      "fallback": "one deterministic GFM-to-HTML render, shared by the F# client and server renderers and re-implemented byte-identically by every other host; pinned by its own corpus at `markdown/corpus.json` and its own cross-host gate",
      "rich": {
        "class": "clientOnly",
        "technique": "KaTeX over inline `$...$` / `$$...$$` spans",
        "seam": "a post-hydration pass over `.fuaran-markdown`; the deterministic renderer leaves those spans as escaped literal text, which is the precondition that keeps the enhancement outside the byte-diff",
        "meaning": "changes the DOM after hydration and is excluded from every parity comparison by contract"
      },
      "fixtures": [
        "nodes/markdown-1.json"
      ],
      "obligations": [],
      "contract": "Phases 292, 293; WIRE_FORMAT.md 14; docs/MARKDOWN.md"
    },
    {
      "kind": "Math",
      "sensitive": true,
      "source": "the LaTeX `source` string plus `display` (inline | block) - deterministic, parity-clean data on the wire; the wire never carries rendered math",
      "fallback": "native MathML for the closed LaTeX subset (real superscripts, subscripts and fractions with NO JavaScript), or the raw escaped source in a `fuaran-math-source` span for out-of-subset input, both inside a `fuaran-math-{block,inline}` container carrying `data-math-display` and `data-fuaran-math-src`. Byte-identical across every renderer; the subset and its byte-exact fixture table are normative in docs/MATH-DEGRADATION.md",
      "rich": {
        "class": "clientOnly",
        "technique": "KaTeX",
        "seam": "a post-hydration pass targeting the `.fuaran-math` container, reading the LaTeX from `data-fuaran-math-src` and replacing the floor wholesale; never emitted by a renderer",
        "meaning": "changes the DOM after hydration and is excluded from every parity comparison by contract"
      },
      "fixtures": [
        "nodes/math-1.json"
      ],
      "obligations": [],
      "contract": "Phases 293, 658; WIRE_FORMAT.md 3.2; docs/SSR.md; docs/MATH-DEGRADATION.md"
    },
    {
      "kind": "Media",
      "sensitive": true,
      "source": "the src binding, the mandatory accessible label, the controls / loop declarations, and the MediaKind variant — Video, carrying autoplay and an optional poster binding, or Audio, carrying neither",
      "fallback": "a real `<video class=\"fuaran-media fuaran-media-video\">` or `<audio class=\"fuaran-media fuaran-media-audio\">` a browser plays with no script. The sanitised `src` (unknown or dangerous schemes collapse to the refusal URL) and an `aria-label` carrying the resolved label are ALWAYS emitted — the label is mandatory on the wire and a transport has no decorative case. `controls` emits unless the document switches it off; `loop` only when declared. A `poster` passes the same URL-scheme + egress floor as `src` and a refused one is DROPPED rather than emitted, because a `<video>` with no poster shows its first frame while a poster at the refusal URL is a broken image over the player. `autoplay` is emitted ONLY together with `muted` — the pairing is what the declaration means, not a default, which is why the wire carries no separate muted slot to fall out of step with it. The Audio variant has NO autoplay pathway at all: the case declares no such slot, so there is nothing for a renderer to read",
      "rich": {
        "class": "none",
        "meaning": "no client-only tier - the parity-checked fallback is the whole render"
      },
      "fixtures": [
        "nodes/media-video-1.json",
        "nodes/media-video-poster-1.json",
        "nodes/media-video-autoplay-1.json",
        "nodes/media-audio-1.json",
        "nodes/media-video-captions-1.json",
        "nodes/media-video-tracks-2.json",
        "nodes/media-audio-transcript-1.json"
      ],
      "obligations": [
        {
          "id": "accessible-name-always",
          "statement": "an `aria-label` carrying the resolved label is ALWAYS emitted — the label is mandatory on the wire and a transport has no decorative case",
          "section": "WIRE_FORMAT.md 3.6.6"
        },
        {
          "id": "autoplay-muted-pairing",
          "statement": "`autoplay` is emitted ONLY together with `muted`, and `muted` rides `autoplay` — the pairing is what the declaration means, not a default, which is why the wire carries no separate muted slot to fall out of step with it",
          "section": "WIRE_FORMAT.md 3.6.6"
        },
        {
          "id": "no-autoplay-pathway",
          "statement": "the Audio variant has NO autoplay pathway at all: the case declares no such slot, so an `<audio>` emission carries neither `autoplay` nor `muted`",
          "section": "WIRE_FORMAT.md 3.6.6"
        },
        {
          "id": "refused-source-dropped",
          "statement": "a `poster` or a `track` source the URL-scheme + egress floor refuses is DROPPED rather than emitted, because a `<video>` with no poster shows its first frame while a poster at the refusal URL is a broken image over the player, and a `<track>` at the refusal URL is a caption menu entry that opens onto nothing",
          "section": "WIRE_FORMAT.md 3.6.6"
        },
        {
          "id": "authored-child-order",
          "statement": "`<track>` children are emitted in the AUTHORED order the wire carries, never re-sorted - the opposite of `srcSet`, because a browser picks one candidate from a srcset by an algorithm while a reader picks a track from a menu built in document order",
          "section": "WIRE_FORMAT.md 3.6.6"
        },
        {
          "id": "single-default-per-kind",
          "statement": "at most one `<track>` of a given `kind` carries `default`: the FIRST election of a kind is honoured and a later one is emitted without the attribute, the track itself still emitted",
          "section": "WIRE_FORMAT.md 3.6.6"
        },
        {
          "id": "transcript-disclosure-named",
          "statement": "a declared `transcript` renders as a `<details>` disclosure BESIDE the transport, carrying the media's resolved label as its accessible name - never as a child of the media element, where a browser would treat it as fallback content and never show it",
          "section": "WIRE_FORMAT.md 3.6.6"
        }
      ],
      "contract": "Phase 1076; WIRE_FORMAT.md 3.6.6; docs/SSR.md (media)"
    },
    {
      "kind": "Embed",
      "sensitive": true,
      "source": "the src binding, the mandatory accessible title, an optional declared aspect ratio, and the closed list of sandbox relaxations - empty by default, which is total denial",
      "fallback": "a real `<iframe class=\"fuaran-embed\">` a browser loads with no script. The `sandbox` attribute is ALWAYS emitted and is EMPTY when the document grants nothing, which is the maximally-restrictive value; `allow-scripts`, `allow-same-origin` and `allow-forms` appear only where declared, de-duplicated and in the vocabulary's declaration order so the markup is deterministic whatever order the document authored. Fullscreen is NOT a sandbox token - it is a permissions-policy directive riding an `allow` attribute emitted only when declared. A `title` carrying the resolved title is ALWAYS emitted: it is mandatory on the wire and a browsing context has no decorative case. `loading=\"lazy\"` and a conservative `referrerpolicy` are unconditional. The `src` passes the `embed` egress class - `https` only, no other scheme and no schemeless reference - and a refused one omits the attribute entirely rather than pointing the frame at the refusal URL, the refusal still recorded as a data attribute. A declared aspect ratio is a CLASS on the frame; no value from the tree ever reaches a style attribute",
      "rich": {
        "class": "none",
        "meaning": "no client-only tier - the parity-checked fallback is the whole render"
      },
      "fixtures": [
        "nodes/embed-1.json",
        "nodes/embed-aspect-1.json",
        "nodes/embed-permissions-1.json"
      ],
      "obligations": [
        {
          "id": "accessible-name-always",
          "statement": "a `title` attribute carrying the resolved title is ALWAYS emitted - the title is mandatory on the wire and a browsing context has no decorative case",
          "section": "WIRE_FORMAT.md 3.6.8"
        },
        {
          "id": "sandbox-always-exactly-declared",
          "statement": "`sandbox` is emitted on every embed and is EMPTY when no permission is declared; the tokens it carries are exactly the declared relaxations, de-duplicated and in declaration order, and `AllowFullscreen` rides `allow` rather than `sandbox`",
          "section": "WIRE_FORMAT.md 3.6.8"
        },
        {
          "id": "refused-embed-source-omitted",
          "statement": "a `src` the `embed` egress class refuses omits the attribute entirely - an `<iframe>` at the refusal URL renders that page, where one with no `src` is an empty frame that fetches nothing - and the refusal is recorded as the egress-refusal data attribute",
          "section": "WIRE_FORMAT.md 19.1"
        }
      ],
      "contract": "Phase 1111; WIRE_FORMAT.md 3.6.8, 19.1; docs/SSR.md (embed)"
    },
    {
      "kind": "Metric",
      "sensitive": false,
      "source": "the label + value source + format",
      "fallback": "the resolved, formatted metric tile",
      "rich": {
        "class": "none",
        "meaning": "no client-only tier - the parity-checked fallback is the whole render"
      },
      "fixtures": [],
      "obligations": [],
      "contract": "WIRE_FORMAT.md 3.2; docs/SSR.md (per-kind server behaviour)"
    },
    {
      "kind": "Modal",
      "sensitive": true,
      "source": "the child tree, the `open` `Binding<bool>`, the heading / dismissable declarations, and the modality with its anchor",
      "fallback": "rendered INLINE at its tree position - no portal, ever, in either modality. A BLOCKING modal is CSS-positioned (`position: fixed`, `z-index: var(--fuaran-z-modal)`) over a backdrop, with `role=\"dialog\"` + `aria-modal=\"true\"`. A POPOVER carries the `fuaran-popover` family, `role=\"dialog\"` and NO `aria-modal`, no backdrop rule and no positioning at all in the stylesheet - the static floor is the surface IN FLOW at its own document position, with the declared anchor riding as a data attribute that records the id was read. A closed surface stays in the DOM behind the native `[hidden]` attribute rather than being omitted, in both modalities",
      "rich": {
        "class": "behavioural",
        "enhancement": "the modal's focus trap, restore-focus and Esc-to-dismiss; the popover's anchored placement, edge flip and light dismiss",
        "seam": "attached on hydration and applied imperatively to the mounted element; behaviour and inline geometry, not structure, so neither can cause a hydration mismatch",
        "meaning": "attached on hydration; adds behaviour, never DOM - it cannot cause a hydration mismatch, so it stays inside the parity contract"
      },
      "fixtures": [
        "nodes/modal-1.json",
        "nodes/popover-anchored-1.json",
        "nodes/popover-open-1.json"
      ],
      "obligations": [
        {
          "id": "aria-modal-only-when-blocking",
          "statement": "`aria-modal=\"true\"` is emitted for `modality: Modal` and never for `modality: Popover`; both carry `role=\"dialog\"`, and the popover carries no scrim element for the same reason - the page behind it is genuinely still there",
          "section": "WIRE_FORMAT.md 3.6.11"
        }
      ],
      "contract": "Phase 289; Phase 1119; WIRE_FORMAT.md 3.2 + 3.6.11; docs/SSR.md (overlay + overflow render-fidelity contract)"
    },
    {
      "kind": "Mount",
      "sensitive": false,
      "source": "the guest tree reference and the isolation boundary declaration",
      "fallback": "the guest subtree rendered within its isolation boundary; the host-bubble channel is host composition, not a rendered affordance",
      "rich": {
        "class": "none",
        "meaning": "no client-only tier - the parity-checked fallback is the whole render"
      },
      "fixtures": [],
      "obligations": [],
      "contract": "WIRE_FORMAT.md 3.2; docs/SSR.md (per-kind server behaviour)"
    },
    {
      "kind": "Progress",
      "sensitive": false,
      "source": "the value source + max",
      "fallback": "the resolved progress element",
      "rich": {
        "class": "none",
        "meaning": "no client-only tier - the parity-checked fallback is the whole render"
      },
      "fixtures": [],
      "obligations": [],
      "contract": "WIRE_FORMAT.md 3.2; docs/SSR.md (per-kind server behaviour)"
    },
    {
      "kind": "ScrollArea",
      "sensitive": true,
      "source": "the child tree, the scroll orientation, and the optional pixel bounds",
      "fallback": "a `fuaran-scrollarea-{vertical,horizontal,both}` overflow container whose clip and scrollbar are entirely CSS-owned, with a structural `role=\"region\"` and a LOWERCASE `tabindex=\"0\"` scroll target (lowercase deliberately: the camelCase form diverges from what React normalises the client's `tabIndex` to). Pixel bounds render as an identical inline `max-height`/`max-width` on both sides",
      "rich": {
        "class": "none",
        "meaning": "no client-only tier - the parity-checked fallback is the whole render"
      },
      "fixtures": [
        "nodes/scroll-1.json"
      ],
      "obligations": [],
      "contract": "Phase 289; WIRE_FORMAT.md 3.2; docs/SSR.md (overlay + overflow render-fidelity contract)"
    },
    {
      "kind": "Select",
      "sensitive": false,
      "source": "the options, the bound value and the multiple flag",
      "fallback": "the `<select>` (with the `multiple` attribute and no scalar `value` in multi mode) carrying its classes and resolved selection, inert server-side",
      "rich": {
        "class": "behavioural",
        "enhancement": "selection write-back is wired on hydration",
        "seam": "the control is inert server-side (Phase 143); the declarative floor is write-back to the control's own writable binding",
        "meaning": "attached on hydration; adds behaviour, never DOM - it cannot cause a hydration mismatch, so it stays inside the parity contract"
      },
      "fixtures": [],
      "obligations": [],
      "contract": "Phase 291; docs/SSR.md (Input - rendered inert)"
    },
    {
      "kind": "Skeleton",
      "sensitive": false,
      "source": "the skeleton shape declaration",
      "fallback": "the full structural placeholder element",
      "rich": {
        "class": "none",
        "meaning": "no client-only tier - the parity-checked fallback is the whole render"
      },
      "fixtures": [],
      "obligations": [],
      "contract": "WIRE_FORMAT.md 3.2; docs/SSR.md (per-kind server behaviour)"
    },
    {
      "kind": "Sparkline",
      "sensitive": true,
      "source": "the series values",
      "fallback": "the `fuaran-sparkline` hook element plus an em-dash placeholder - a readable, deterministic stand-in rather than a blank",
      "rich": {
        "class": "clientOnly",
        "technique": "the polyline is drawn",
        "seam": "hydration",
        "meaning": "changes the DOM after hydration and is excluded from every parity comparison by contract"
      },
      "fixtures": [
        "nodes/spark-1.json"
      ],
      "obligations": [],
      "contract": "docs/SSR.md (Display.Sparkline)"
    },
    {
      "kind": "SplitPanel",
      "sensitive": false,
      "source": "the two child subtrees + the split declaration",
      "fallback": "the full structural container, classes identical on both sides",
      "rich": {
        "class": "none",
        "meaning": "no client-only tier - the parity-checked fallback is the whole render"
      },
      "fixtures": [],
      "obligations": [],
      "contract": "WIRE_FORMAT.md 3.2; docs/SSR.md (per-kind server behaviour)"
    },
    {
      "kind": "Stepper",
      "sensitive": false,
      "source": "the step declarations and the active-step binding",
      "fallback": "the full structural step list with the active step marked, ARIA-complete",
      "rich": {
        "class": "behavioural",
        "enhancement": "step selection is wired on hydration",
        "seam": "the control is inert server-side (Phase 143)",
        "meaning": "attached on hydration; adds behaviour, never DOM - it cannot cause a hydration mismatch, so it stays inside the parity contract"
      },
      "fixtures": [],
      "obligations": [],
      "contract": "docs/SSR.md (Layout)"
    },
    {
      "kind": "SummaryList",
      "sensitive": false,
      "source": "the summary rows",
      "fallback": "the full structural list, classes identical on both sides",
      "rich": {
        "class": "none",
        "meaning": "no client-only tier - the parity-checked fallback is the whole render"
      },
      "fixtures": [],
      "obligations": [],
      "contract": "WIRE_FORMAT.md 3.2; docs/SSR.md (per-kind server behaviour)"
    },
    {
      "kind": "Switch",
      "sensitive": false,
      "source": "the selector binding and the per-case child subtrees",
      "fallback": "the case matching the resolved initial value, else the default. The client's first render reads the same seeded value, so both sides emit the same tree shape",
      "rich": {
        "class": "none",
        "meaning": "no client-only tier - the parity-checked fallback is the whole render"
      },
      "fixtures": [],
      "obligations": [],
      "contract": "WIRE_FORMAT.md 3.2; docs/SSR.md (per-kind server behaviour)"
    },
    {
      "kind": "Tabs",
      "sensitive": false,
      "source": "the tab declarations, their panels, and the active-tab binding",
      "fallback": "a static `role=\"tablist\"` plus the active panel, ARIA-complete",
      "rich": {
        "class": "behavioural",
        "enhancement": "keyboard navigation and tab switching are wired on hydration",
        "seam": "the control is inert server-side (Phase 143); switching a tab after hydration is interaction, not a fidelity tier",
        "meaning": "attached on hydration; adds behaviour, never DOM - it cannot cause a hydration mismatch, so it stays inside the parity contract"
      },
      "fixtures": [],
      "obligations": [],
      "contract": "docs/SSR.md (Layout.Tabs)"
    },
    {
      "kind": "Toast",
      "sensitive": true,
      "source": "the content, the `open` `Binding<bool>`, and the tone",
      "fallback": "rendered INLINE at its tree position with no portal, always emitted, `role=\"status\"` + `aria-live=\"polite\"`, and `[hidden]` when closed rather than absent. This is the DECLARATIVE, hydration-stable notification surface; the imperative `Action.Notify` path renders no node at all and is a host-chrome trigger, not a tier of this kind",
      "rich": {
        "class": "none",
        "meaning": "no client-only tier - the parity-checked fallback is the whole render"
      },
      "fixtures": [
        "nodes/toast-1.json"
      ],
      "obligations": [],
      "contract": "Phase 289; WIRE_FORMAT.md 3.2 (Toast vs Action.Notify); docs/SSR.md"
    }
  ]
}
