{
  "version": 1,
  "description": "Fuaran conformance-law vector corpus. SELF-ENUMERATED: these vectors are deliberately not indexed by the corpus root manifest.json, which indexes the canonical wire-format codec families only — the merge-conformance/ and devtools-relay/ precedent. A `law-vectors` file carries the (input, expected) pairs one Fuaran.Core conformance law family draws from a declared seed, computed by calling the pinned kit, so a host that is not the reference can run the same family over the same sample. A family that takes the host's own implementation as an argument (a PARITY family) carries the REFERENCE ANSWERS the host is compared against, and declares its sample generator in its own description, since the family itself has no sample to draw. These are BEHAVIOUR vectors, not byte-parity fixtures: a host asserts the verdicts and derived values, not the framing of this file.",
  "families": [
    { "id": "capabilityLaws", "kind": "law-vectors", "file": "capability-laws.json", "kitVersion": "0.18.0", "seed": 20260904, "iterations": 12, "vectors": 72, "description": "The invocable-capability contract: arg validation, the capture-replay key, the declaration codec round-trip, and id-stable registry enumeration." },
    { "id": "transformLaws", "kind": "law-vectors", "file": "transform-laws.json", "kitVersion": "0.19.0", "seed": 20260904, "iterations": 16, "vectors": 16, "description": "The dataframe-transform parity contract: for each drawn (source, pipeline), the canonical wire encoding of the table the reference evaluator produced — or the bare verdict that it refused the pipeline. A host with its own evaluator decodes both halves with its existing dataframe codec, evaluates, and compares. Exported as the reference's ANSWERS rather than as the law's verdict, which is why this family was previously listed as not exported: run over the reference itself the law certifies only the reference's self-consistency, whereas its answers are exactly what a host cannot obtain without the reference." }
  ],
  "notExported": []
}
